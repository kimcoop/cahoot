
.container
  .row
    .span12
      %h1 Lists

%table.table.table-bordered.table-hover
  %tr
    %th Name
    %th Description
    %th Created By
    %th Created On
    %th
    %th
    %th

  - @lists.each do |list|
    %tr
      %td= list.name
      %td= list.description
      %td= list.user.full_name
      %td= list.created_at.strftime('%b %d, %Y')
      %td= link_to 'Show', list
      %td= link_to 'Edit', edit_list_path(list)
      %td= link_to 'Delete', list, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New List', new_list_path