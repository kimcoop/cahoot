= simple_form_for(@list) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :description
    = f.input :user_id
    = f.input :owner_id

  .form-actions
    = f.button :submit
