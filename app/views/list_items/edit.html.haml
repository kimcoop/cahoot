%h1 Editing List Item


= simple_form_for @list_item do |f|
	= f.input :name
	= f.input :description
	=	f.association :owner, collection: User.all(order: 'first'), prompt: "Assign Item To"
	= f.button :submit


= link_to 'Show', @list_item
\|
= link_to 'Back', list_items_path
